import { motion } from "framer-motion";
type Props={eyebrow?:string;title:string;description?:string;align?:"left"|"center";as?:"h1"|"h2"};
export function SectionHeading({eyebrow,title,description,align="left",as="h2"}:Props){const Heading=as;return <motion.div initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:"-50px"}} transition={{duration:.5}} className={`max-w-3xl ${align==="center"?"mx-auto text-center":""}`}>{eyebrow&&<p className={`eyebrow mb-3 ${align==="center"?"justify-center":""}`}>{eyebrow}</p>}<Heading className="font-display text-3xl md:text-4xl lg:text-5xl font-extrabold tracking-tight text-ink">{title}</Heading>{description&&<p className="mt-4 text-base md:text-lg text-muted leading-relaxed">{description}</p>}</motion.div>;}
