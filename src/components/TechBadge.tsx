import * as Lucide from "lucide-react";
import type { Technology } from "../data/site";
export function TechBadge({tech}:{tech:Technology}){const Icon=(Lucide as any)[tech.icon]??Lucide.Box;return <div title={`${tech.name} — ${tech.category}: ${tech.why}`} className="group inline-flex items-center gap-2 px-3.5 py-2 rounded-full bg-white border border-line hover:border-brand/50 hover:bg-surface transition-all"><Icon size={14} className="text-brand"/><span className="text-sm font-semibold text-ink">{tech.name}</span><span className="text-[10px] font-mono uppercase tracking-widest text-muted group-hover:text-brand">{tech.category}</span></div>;}
